[project]
name = "analise-cv-univesp"
version = "0.1.0"
description = "Sistema web para geracao automatizada de curriculos profissionais"
requires-python = ">=3.10"

dependencies = [
    "flask>=3.0",
    "fpdf2>=2.7",
    "openai>=1.0",
    "python-dotenv>=1.0",
]

[tool.setuptools]
packages = []
